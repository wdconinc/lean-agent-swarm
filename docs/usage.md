# Usage

To run the framework locally:

1. **Start Redis:**
```bash
./bin/start_redis.sh
```

2. **Run the Planner:**
```bash
./bin/run_planner.sh
```

3. **Run the Worker:**
```bash
./bin/run_worker.sh
```
